### filter_data_preference.py : For filtering preference weights in the datasets

This code is only used for the relevant Appendix section, not for any of the main results!

Before use: ensure you filter based on the intented lower and higher bound of preference weights, all that fall in between the bound will be removed. Change `path` and `new_path` as desired.

### get_results_filtered.py : Obtain the average NSW for your desired seeds over all results

### visualize_data.ipynb : Visualise the data distribution of your desired dataset